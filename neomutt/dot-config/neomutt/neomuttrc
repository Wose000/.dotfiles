set from = "jiro.due@gmail.com"
set realname = "Wose"
set imap_authenticators = "Login"
set imap_user = "jiro.due@gmail.com"
set imap_pass = "`pass show mutt/jiro.due@gmail.com`"

set folder = "imaps://imap.gmail.com:993"
set spoolfile = "+INBOX"
set record = "+Sent"
set postponed = "+Drafts"

# SSL/TLS settings for IMAP
set ssl_starttls = yes    # Use STARTTLS for secure connection if supported
set ssl_force_tls = yes   # Force TLS negotiation
set ssl_verify_host = yes # Verify the server's certificate

# SMTP Configuration (Outgoing Mail)
set smtp_authenticators = "Login" # Or "login"
set smtp_url = "smtp://smtp.gmail.com:587/"
set smtp_user = "jiro.due@gmail.com"
set smtp_pass = "`pass show mutt/jiro.due@gmail.com`"
