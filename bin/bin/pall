#!/bin/env bash

cd $HOME/m/doc/note/
git add .
git commit -m "fast update"
git push origin main

cd  $HOME/.dotfiles/nvim/dot-config/nvim/
git add .
git commit -m "fast update"
git push origin main

cd  $HOME/.dotfiles/awesome/dot-config/awesome/
git add .
git commit -m "fast update"
git push origin main

cd $HOME/.dotfiles
git add .
git commit -m "fast update"
git push origin main
